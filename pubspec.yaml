name: bookclub.app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

  # 2024.08.15 로그인 페이지 작성을 위한
  #   firebase_core: latest_version
  #  firebase_auth: latest_version
  #  google_sign_in: latest_version
  #  flutter_kakao_login: latest_version # Kakao login
  #  naver_login: latest_version         # Naver login

  firebase_auth: ^4.16.0  # Firebase 인증
#  flutter_naver_login: ^2.0.0-rc.1  # 네이버 로그인
#  kakao_flutter_sdk: ^0.6.3  # 카카오 로그인

  cupertino_icons: ^1.0.8
  firebase_core: ^2.24.2
  google_sign_in: ^6.2.1
  cloud_firestore: ^4.14.0

# 2024.08.23 이미지 첨부 관련 패키지 설치
  image_picker: ^0.8.4+3
  firebase_storage: ^11.6.0
#  firebase_app_check: ^0.2.1+8
  table_calendar: ^3.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:

  uses-material-design: true






